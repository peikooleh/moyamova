# MOYAMOVA 2.0

**MOYAMOVA** — офлайн-тренажёр для изучения иностранных языков на карточках.  
Работает прямо в браузере, без регистрации, без рекламы и подписок.  

- 📱 Оптимизировано под мобильные устройства (портретный режим)
- 📴 Работает офлайн через PWA + Service Worker
- 🧠 Акцент на запоминании слов и устойчивом прогрессе
- 💾 Резервные копии: экспорт / импорт прогресса одним файлом

---

## 🚀 Что умеет приложение

### 🎴 Карточки слов и словари

- Готовые словари по нескольким языкам (DE, EN, FR, ES, SR и др.)
- Раздел **«Словари»**:
  - список доступных словарей;
  - прогресс по каждому словарю (сколько слов выучено);
  - переключение между языками через флаги.
- Каждое слово можно:
  - выучить через тренажёр;
  - добавить в избранное;
  - при ошибке оно автоматически попадает в «Мои ошибки».

---

### ⭐ Тренажёр и прогресс

- Алгоритм на основе **звёздочек**:
  - у каждого слова есть уровень освоения от `0` до `starsMax`;
  - тренажёр повышает или понижает уровень в зависимости от ответа;
  - слово считается *выученным*, когда достигает максимального уровня звёзд.
- Бонус-логика:
  - отдельная работа с сериями ошибок по слову;
  - «cooldown» — защита от бессмысленного фарма звёзд, когда подряд много ошибок.

---

### 📚 Избранное и Мои ошибки

- **Избранное**:
  - отдельный экран со словами, которые пользователь отметил вручную;
  - удобно использовать как «личный словарик».
- **Мои ошибки**:
  - виртуальный словарь, собирающий слова, по которым были неправильные ответы;
  - помогает целенаправленно отработать слабые места;
  - отображается в том же стиле таблиц, что и «Словари» и «Избранное».

---

### 📊 Статистика изучения

Экран **«Статистика»** показывает прогресс по языкам:

- Переключение между языками через флаги (как в «Словарях»).
- Для каждого языка:
  - общее количество слов в словарях;
  - сколько из них выучено;
  - сколько словарей начато и завершено;
  - распределение выученных слов по частям речи.

Визуально:

- два крупных набора **кольцевых диаграмм**:
  1. Основные части речи (глаголы, существительные, прилагательные);
  2. Остальные части речи (наречия, местоимения и т.п.).
- кольца разных цветов, с легендой и значениями `выучено / всего`.

Дополнительно:

- блок «Активность» (круглые точки-индикаторы по дням);
- данные активности считаются и хранятся в `App.state.activity` и восстанавливаются из бэкапа.

---

### 🎨 Интерфейс и режимы

- Светлая и тёмная тема:
  - переключатель в боковом меню;
  - тема сохраняется в настройках.
- Язык интерфейса:
  - RU / UK;
  - переключатель в меню, тексты подхватываются через модуль i18n.
- Режим сложности:
  - обычный / сложный (влияет на логику тренажёра и доп. нагрузку).

---

### 💾 Резервное копирование

- Раздел **«Резервное копирование»** в боковом меню:
  - **Экспорт** — скачивание файла с текущим состоянием словарей, прогресса и настроек;
  - **Импорт** — загрузка ранее сохранённого файла, восстановление состояния.
- В бэкап входят:
  - прогресс по звёздам (`App.state.stars`);
  - служебные поля тренажёра (`wrongStreak`, `cooldownUntil`, `lastSeen` и др.);
  - данные активности (`App.state.activity`);
  - настройки темы, языка интерфейса, уровня сложности и др.

---

## 🧩 Архитектура проекта

Проект построен без крупных фреймворков — **чистый JavaScript**, модульная структура, разбиение по зонам ответственности.

Основные слои:

- `app.core.js` — базовый объект `App`, общие утилиты, версия приложения.
- `dicts.js` + `deck.*.js` — реестр словарей и данные по словам.
- `app.decks.js` — работа со словарями, вычисление языка по ключу, имена словарей и т.п.
- `app.trainer.js` — логика тренажёра, звёзды, ошибки, прохождение сетов.
- `app.favorites.js`, `app.mistakes.js`, `app.decks.bridge.js` — мосты и надстройки для избранного и ошибок.
- `ui.*.js` — UI-поведение:
  - `ui.stats.core.js` — накопление статистики и активности;
  - `ui.lifecycle.js` — жизненный цикл и обновления;
  - `ui.options.safe.js`, `ui.sets.done.js`, `ui.progress.scope.js`, `ui.state.js` и др.
- `view.*.js` — отдельные экраны:
  - `view.dicts.js` — «Словари»;
  - `view.favorites.js` — «Избранное»;
  - `view.mistakes.js` — «Мои ошибки»;
  - `view.stats.js` — «Статистика»;
  - `home.js` — главный экран.
- `app.backup.js` — экспорт/импорт состояния.
- `sw.js` — сервис-воркер для PWA и офлайн-режима.

---

## 🗂 Структура проекта (упрощённо)

```text
.
├── index.html
├── css
│   ├── theme.light.css
│   ├── theme.dark.css
│   └── view.stats.css
├── js
│   ├── app.core.js
│   ├── app.decks.js
│   ├── app.trainer.js
│   ├── app.favorites.js
│   ├── app.mistakes.js
│   ├── app.decks.bridge.js
│   ├── app.backup.js
│   ├── dicts.js
│   ├── deck.de.js
│   ├── deck.en.js
│   ├── deck.fr.js
│   ├── deck.es.js
│   ├── deck.sr.js
│   ├── ui.stats.core.js
│   ├── ui.bus.js
│   ├── ui.lifecycle.js
│   ├── ui.options.safe.js
│   ├── ui.sets.done.js
│   ├── ui.progress.scope.js
│   ├── ui.state.js
│   ├── view.dicts.js
│   ├── view.favorites.js
│   ├── view.mistakes.js
│   ├── view.stats.js
│   ├── home.js
│   ├── theme.js
│   ├── updates.js
│   ├── ga.consent.js
│   └── ui.swipe.js
├── img
│   └── (иконки, логотипы, PWA-иконки)
├── manifest.webmanifest
└── sw.js
